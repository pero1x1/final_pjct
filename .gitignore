.venv/
__pycache__/
*.pyc
.DS_Store
.pytest_cache/

# Local credentials (do not commit)
.aws/

# MLflow local runs/artifacts
mlruns/

# Terraform local state/keys (do not commit)
infra/terraform/**/.terraform/
infra/terraform/**/terraform.tfstate*
infra/terraform/**/sa-key.json

# K8s secrets (create from *.example, do not commit)
k8s/*secret*.yaml
k8s/observability/*secret*.yaml

# Observability secrets/overrides (do not commit)
observability/*secret*.yaml
observability/*values*.local.yaml
observability/*values*.private.yaml
observability/*values*.override.yaml

# Stage 7 (Airflow / orchestration) secrets
orchestration/**/secret*.yaml
airflow/**/secret*.yaml

# Локальные артефакты (не коммитим)
metrics.json
